class Solution {
    public int smallestNumber(int n) {
        for(int i =0 ; i<12 ; i++){
            if(Math.pow(2,i)<=n) continue;
            else{
              
                return (int) Math.pow(2,i)-1;
            }
        }
        return 0;
    }
}
