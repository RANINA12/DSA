class Solution {
    public boolean hasAlternatingBits(int n) {
        String check = Integer.toBinaryString(n);

        for (int i= 0 ; i< check.length()-1;i++){
            if(check.charAt(i)==check.charAt(i+1)) return false;
        }

        return true;
    }
}
